# @aos-harness/provenance-probe

Scratch package. Published once to verify that `bun publish --provenance` inside GitHub Actions produces a real, verifiable npm provenance attestation.

Deprecate immediately after verification:

```
npm deprecate @aos-harness/provenance-probe "scratch — verification only"
```

Result recorded in `docs/security/provenance-probe-result.md`.
